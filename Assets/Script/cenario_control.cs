using UnityEngine;
using System.Collections;

public class cenario_control : MonoBehaviour {


	/**
	public bool primeiro = false;
	public bool deslocar = false;

	void Update () {
	
		if(deslocar){

			if(!primeiro){
				Vector3 aux = new Vector3(this.transform.position.x, this.transform.position.y - 2.56f,this.transform.position.z);
				this.transform.position = aux;
				primeiro = true;
			}else{
				Vector3 aux = new Vector3(this.transform.position.x, this.transform.position.y - 3.099f,this.transform.position.z);

				//transform.Translate(Vector3.down * Time.deltaTime * 188.5f);

				this.transform.position = aux;
			}
			deslocar = false;
		}
	}

*/
}
